# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Rust 构建产物（独立 target 目录）
target/
src-tauri/target

# 环境变量（含 token/密钥，绝不提交）
.env
.env.local
.env.*.local

# 本地私密快捷方式配置（个人机器路径/应用，不提交）
src/config/launchers.local.json
src/config/prompts.local.json

# 首次启动种子数据（个人运行数据：可能含 API key 与对话记录）
# 注意只忽略 *.json，README.md 必须提交——Tauri 的资源 glob 无匹配会构建失败
src-tauri/resources/*.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
